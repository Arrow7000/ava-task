# Ava task by Aron Adler

## Instructions

1. Install dependencies with `yarn install`
1. Start the server with `yarn start`
1. The project should open in the browser on [http://localhost:8000](http://localhost:8000)
